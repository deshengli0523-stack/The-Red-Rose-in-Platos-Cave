# 咨询知识库项目规则

这些规则是安全与语义边界，不由 Prompt、模型判断或赶工例外覆盖。

## 会谈作用域

- 一个 Codex task（任务/对话线程）只能绑定一个 client。首次成功加载后永久绑定；不得在同一 task 切换客户或复用另一客户 handle，不得读取其他客户。
- 只有 `load_client_context` 的初始控制面参数可接收 client ID。绑定后的客户工具只接收 `session_handle` 和领域参数，不接收客户 ID、路径或 SQL。
- 会谈开始时固定 profile 快照；正式 profile 在会谈期间只读。新陈述只进入 session 临时事实，未经会后差异审核不得成为长期事实。
- 检索共享案例时，必须在进入模型上下文前按传递性来源客户全谱系排除当前客户。客户 A 的案例不得用于客户 A，即使经过摘要、合并或派生。

## 每轮实际回复

- 候选回复不是实际回复，模型输出也不是证据。候选不得自动进入 actual conversation、事实、干预记录或归档。
- 上一轮必须记录原样采用、精确编辑后的实际正文，或明确的 `external_reply_unknown`，然后才能接收下一条来访者消息。不得推定候选已发送。
- 回复前执行证据与一致性检查：权限、来源、审核状态、有效期、版本、冲突和前后矛盾必须明确；不允许为求流畅而消除真实不确定性。
- 风险标签、级别、规则名和系统告警仅供咨询师内部观察；来访者文本不得包含这些字段或风险提示模板。

## 正式知识与批准

- 正式 Claim、Wiki、C1、客户事实、归档及删除等写入必须先展示精确 diff，再在目标事务中验证并消费匹配的一次性本地批准。模型、子代理和普通审核不能代替咨询师批准。
- C1 是咨询师自创且已批准理论的最高咨询框架，但不得改写客户事实、原文、当前官方事实、法律或专业硬约束。
- 未注册的工具不得调用或伪造成功。运行时不自动联网摄取资料，也不接受任意知识文件路径。

## 隐私与落盘

- 去标识化必须发生在粘贴到 Codex 前；事后扫描不能撤回已经发送的文本。
- 真实客户正文、原始会谈、稳定身份标识、私有 profile、vault、数据库、向量和模型缓存不得进入 Git、Codex Memory 或 Chronicle。
- 日志、审计、评估和 Git 只保存允许的引用、hash、状态与合成数据，不保存客户正文或可逆身份映射。
- MCP stdout 只承载 STDIO 协议，日志和固定错误只写 stderr；required 服务不可静默降级为无知识回答。

## 执行方式

- Superpowers 插件的全部 Skill 只按需调用：仅在用户明确点名，或当前任务与 Skill description 清晰匹配且能实质改善结果时选择最小集合。
- 不得因每轮对话、每个 task、每次提交或准备宣布完成而自动加载 Superpowers Skill；普通咨询文本不触发开发流程 Skill。
- 除高风险、不可逆或会扩大授权边界的决策外，按已批准设计代理执行并验证，不反复请求确认。
